# Anthropic — required for AI analysis
ANTHROPIC_API_KEY=sk-ant-...

# Email — required for PDF delivery
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@rflct.be
SMTP_PASS=your-smtp-password
SMTP_FROM="RFLCT Advertentie-scan <noreply@rflct.be>"
RFLCT_EMAIL=info@rflct.be

# App
NEXT_PUBLIC_BASE_URL=https://scan.rflct.be

# Scan data directory (writable path for JSON persistence)
SCAN_DATA_DIR=./data/scans
